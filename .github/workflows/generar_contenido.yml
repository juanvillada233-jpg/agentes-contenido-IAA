import os
from copy_agent import generar_copy_experto

def tarea_diaria():
    # Eliges el tema según lo que quieras publicar hoy
    # Puedes rotarlos o elegir uno fijo
    tema_de_hoy = "resiliencia" 
    
    print(f"--- Iniciando generación de: {tema_de_hoy} ---")
    
    try:
        frase_final = generar_copy_experto(tema_de_hoy)
        print(f"Resultado para la imagen: \n{frase_final}")
        
        # Aquí iría tu código existente para subir a Google Drive
        # ... (mantén tu lógica de Drive aquí)
        
    except Exception as e:
        print(f"Error en el flujo: {e}")

if __name__ == "__main__":
    tarea_diaria()
